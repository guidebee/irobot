apply plugin: 'com.android.application'

android {
    namespace = 'com.guidebee.irobot'
    compileSdk = 36
    defaultConfig {
        applicationId = "com.guidebee.irobot"
        minSdkVersion 21
        targetSdkVersion 36
        versionCode 15
        versionName "1.0.0"
        testInstrumentationRunner = "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
        }
    }
    buildFeatures {
        buildConfig = true
        aidl = true
    }
    lint {
        disable 'UseRequiresApi'
    }
}

dependencies {
    testImplementation 'junit:junit:4.13.2'
}

apply from: "$project.rootDir/config/android-checkstyle.gradle"
